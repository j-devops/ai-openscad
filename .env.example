# AI-OpenSCAD Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# REQUIRED: OpenAI Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================
# Application URLs
# ============================================
# Frontend will use these to connect to backend
# For local development, use localhost
# For production, use your domain
VITE_API_URL=http://localhost:8100
VITE_WS_URL=ws://localhost:8100

# ============================================
# Environment
# ============================================
# Options: development, production
ENVIRONMENT=development

# ============================================
# Application Configuration
# ============================================
APP_NAME=AI-OpenSCAD
APP_VERSION=0.1.0

# ============================================
# Redis Configuration
# ============================================
# Default uses the redis service from docker-compose
# For production, use managed Redis (AWS ElastiCache, Redis Cloud, etc.)
REDIS_URL=redis://redis:6379

# Optional: Redis password (recommended for production)
# REDIS_PASSWORD=your-secure-password

# ============================================
# Backend Configuration
# ============================================
# Where generated files are stored (inside container)
DATA_PATH=/app/data

# CORS origins (comma-separated, no spaces)
# For development, localhost is fine
# For production, add your frontend domain
CORS_ORIGINS=http://localhost:4100,http://localhost:3000

# ============================================
# Optional: Advanced Configuration
# ============================================

# OpenAI Model (default: gpt-4-turbo-preview)
# OPENAI_MODEL=gpt-4-turbo-preview

# OpenAI Temperature (0.0-1.0, lower = more deterministic)
# OPENAI_TEMPERATURE=0.3

# Max tokens for generation
# OPENAI_MAX_TOKENS=2000

# Render timeout (milliseconds)
# RENDER_TIMEOUT=120000

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
